Implemented caching. By default, each download of remote meshes will write the mesh in the system cache directory. If a remote mesh is already present in the cache directory, the `write` method will not download the mesh from Zenodo, but rather copy it from cache to destination.
