Fixed and better documented mesh initialization logic. First meshes are searched locally, interpreting the mesh name as its path. If they are not found, the mesh name is looked up in the Zenodo record.
