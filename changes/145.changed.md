The entity used for the first magnetocrystalline anisotropy constant is now `MagnetocrystallineAnisotropyConstantK1` (`K1`) instead of `UniaxialAnisotropyConstant` (`Ku`). The entity `Ku` is however also accepted in the hysteresis loop and will be read internally as a `K1` instead.
