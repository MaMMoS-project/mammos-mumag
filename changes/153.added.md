Now running any method of a `mammos_mumag.simulation.Simulation` object will list all the missing attributes and not just the first one. The method that does so, `mammos_mumag.simulation.check_attribute` was now hidden from the public API.
