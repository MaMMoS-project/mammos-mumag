The backend of the micromagnetic calculation has now drastically changed. Instead of assembling the finite element matrices with `esys-escript`, we now operate via a "matrix-free" method, i.e. exploiting parallel calculation to evaluate integrals at each time step without storing matrices of very high dimension.
