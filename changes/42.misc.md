Added `examples/hysteresis.ipynb` to document full functionality of `mammos-mumag` when running a hysteresis loop simulation. Additionally, show the functionality of the package irrelevant to an average user in `examples/additional-functionality.ipynb`.
